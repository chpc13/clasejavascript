//try y catch
try {
    const nombre= "stiven";
    const apellidos = "Pareja Jimenez";
    const nombreCompleto = nombre + " " + apellidos;
    console.log(nombreCompleto);

} catch (e) {
    console.log(e)
}